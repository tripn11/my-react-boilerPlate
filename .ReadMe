the .env.firebase file must be updated with the current values of the database created.
babel loader docs instructed the addition of options and presets something i have already done in a seperate file, don't know if i should repeat it 
was adviced to download one html plugin which would keep generating a different html file
transform-class-properties get issues
had to download corejs and import at appjs to replace babel polyfills
adviced to add npm i --save-dev @types/core-js for the core-js declaration